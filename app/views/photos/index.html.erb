<h1>All Photos</h1>
<%= link_to "Upload New Photo", new_photo_path %>

<ul>
  <% @photos.each do |photo| %>
    <li>
      <%= link_to photo.title, photo %>
      (<%= link_to "Comments", polymorphic_path([photo, :comments]) %>)
    </li>
  <% end %>
</ul>
